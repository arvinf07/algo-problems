// Takes in two strings 
// return true if they are anagrams
// false if they are not 
// anagram: 2 words with same amount and type of characters

// split strings into arrays and sort them asc
// compare them against each other
// if they have the same type and number of characters return true
// Otherwise return false

// split both strings into arrays and store them in vars
// sort both String Arrays 
// iterate through one stringArr
  // check if the currentChar is equal to char in the other array in the same position
  // if not return false
// return true

// <---->

// if string1.length != string2.length return false
// initialize 2 empty objects
// iterate through the first string
  // if char does not exist in the obj
    // obj[char] = 1
  // else 
    // obj[char]++
// 

// iterate through the second string
  // if char does not exist in the obj
    // obj[char] = 1
  // else 
    // obj[char]++
// 

// iterate through the keys 
  //  if obj1[key] =! obj2[key] return false
// return true

